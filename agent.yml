path.data: data
path.logs: log

metrics:
  network:
    metrics:
      - summary
      - interface
    interfaces:
  #    - lo0
      - en1
  memory:
    metrics:
      - swap
      - memory
  disk:
    metrics:
      - ioqs
      - throughput
  cpu:
    metrics:
      - idle
      - system
      - user
      - iowait
      - load
  elasticsearch:
    discovery: true
    metrics:
      - summary
      - indices
      - nodes

labels:
  env: dev

tags:
  - linux
  - x86
  - es7
  - v7.5